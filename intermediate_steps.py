def freight_in(steel_width: int, steel_weight: int) -> int:

    # Calculate total price for freight in cost

    return 1

def storage(steel_width: int, steel_weight: int) -> int:

    # Calculate total price for storage cost

    return 1

def loading_unloading(steel_width: int, steel_weight: int) -> int:

    # Calculate total price for loading/unloading cost

    return 1

def packaging(steel_width: int, steel_weight: int) -> int:

    # Calculate total price for packaging cost

    return 1

def freight_out(steel_width: int, steel_weight: int) -> int:

    # Calculate total price for storage cost

    return 1

